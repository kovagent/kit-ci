<!--
Thin crates.io / docs.rs front page. The full README.md (sources, methodology,
recipes) stays in the repo and is NOT shipped to the registry. Point the lib
crate at this file with `readme = "../../README.crates.md"` and exclude the rich
docs + bundled data from the package. Keep this short: purpose, install, one
minimal example, a link to the repo for full docs, license. No source-priority
waterfalls, no methodology, no competitive detail. Math-methodology citations
(Black-Scholes, Lee-Ready, etc.) may stay.
-->
# REPLACE_KIT

REPLACE_ONE_LINE_PURPOSE

```toml
[dependencies]
REPLACE_KIT = "REPLACE_VERSION"
```

```rust,no_run
// minimal usage — one call, no methodology
REPLACE_MINIMAL_EXAMPLE
```

Full documentation: <https://github.com/userFRM/REPLACE_KIT>

Licensed under MIT OR Apache-2.0.
